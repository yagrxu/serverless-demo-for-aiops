fastapi==0.115.0
# mcp>=1.27 requires uvicorn>=0.31.1; keep floor there.
uvicorn[standard]>=0.31.1
langgraph>=0.2.34
langchain-aws>=1.0.0
# Pin mcp + langchain-mcp-adapters together. Newer langchain-mcp-adapters
# imports `streamable_http_client` (new name in mcp>=1.27), older mcp only
# exposes the deprecated `streamablehttp_client` alias — using `>=` with
# both lets pip resolve an incompatible pair and the container crashes at
# import time before OTel can even initialise.
langchain-mcp-adapters==0.2.2
mcp>=1.27.0,<2.0.0
boto3>=1.35.0
aws-opentelemetry-distro>=0.17.1
opentelemetry-instrumentation-langchain
