## Per-UI files

Each UI is a minimal Vite + React + TS app. Files are tiny; copy
the same package.json / tsconfig into all three and differentiate
the `src/` only. See `chatbot/`, `device-simulator/`, `admin-console/`.
