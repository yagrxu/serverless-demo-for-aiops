# MacOS files
.DS_Store
tmp/

# IDE files
.idea/
.vscode/
*.swp
*.swo

# Local environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Local dev stack runtime state
local/.run/
local/.logs/
node_modules/
cdk.out/
dist/
.omni/
.venv/

# Python cache and compiled files
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
.coverage
.pytest_cache/

# CDK build output directories
cdk.out2/

# Kiro specs (point-in-time design documents)
.kiro/specs/

# Reference material copied from external repos for inspection only
reference/

# Local-only TODO drafts (not part of the spec workflow)
todo/

# Local-only working folders (not pushed to git)
reference/
todo/
.claude/
.github/
.cursor/
.kiro/
.agents/

# Slack integration local secrets (never commit — store in Secrets Manager)
slack/**/*.secret
slack/**/secrets.json
slack/**/.secrets/
slack/tests/.local-config.json

# Hypothesis (pytest property-testing cache)
.hypothesis/
